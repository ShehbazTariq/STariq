# CV Source

This folder contains the LaTeX source for the website CV.

Edit `CV_Shehbaz.tex`, then run from the repository root:

```powershell
.\scripts\build-cv.ps1
```

The script compiles the CV and updates `files/cv.pdf`, which is served from `/cv/`.
